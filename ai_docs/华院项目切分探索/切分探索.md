思考一个问题。如果我想把这个项目缩小，只保留最基础的模型调用能力。比如新写的@backend/app/api/routes/huayuan_chat.py @backend/app/api/routes/huayuan_portrait.py @backend/app/api/routes/huayuan_radar_event.py 的这种所必须的代码模块，项目应该如何切分和验证呢？

为何有这个需求，最近在开发一个公司的项目，需要把当前工程部署到docker中，供其它服务使用。但是当前服务依赖了一些其它的中间件，我觉得太多了，想减少一些组件。

请帮我梳理一下，可行性的报告。