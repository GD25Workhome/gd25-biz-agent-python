# =============================================================================
# requirements.txt — 全量 / 遗留开发依赖
# =============================================================================
# 华院生产镜像请用：requirements-huayuan.txt（见 Dockerfile）
# 依赖裁剪说明：ai_docs/26091601-华院方案A依赖裁剪清单.md
#
# 分区约定：
#   [华院保留]  — 华院运行时也需要（与 requirements-huayuan 对齐）
#   [华院可删]  — 仅医疗 / RAG / AutoGen / 主库 PG；华院镜像不得安装
#   [worker]    — news_content_worker 需要；华院 API 镜像可不装
#   [开发]      — 测试与 lint，不进运行镜像
# =============================================================================

# ---------- [华院保留] Web ----------
fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.23.0,<1.0.0

# ---------- [华院保留] LangGraph / LangChain ----------
langgraph>=1.0.0,<2.0.0
langchain>=1.0.0,<2.0.0
langchain-core>=1.0.0,<2.0.0
langchain-openai>=1.0.0,<2.0.0

# ---------- [华院可删] RAG 文本切分 ----------
langchain-text-splitters>=1.0.0,<2.0.0

# ---------- [华院可删] AutoGen ----------
autogen-agentchat>=0.7.0,<1.0.0
autogen-core>=0.7.0,<1.0.0
autogen-ext[openai]>=0.7.0,<1.0.0

# ---------- [华院可删] 主库 PostgreSQL ----------
sqlalchemy>=2.0.0,<3.0.0
psycopg[binary]>=3.1.0,<4.0.0
psycopg-pool>=3.2.0,<4.0.0
alembic>=1.12.0,<2.0.0

# ---------- [华院保留] 配置 ----------
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
# python-dotenv 仅写在 requirements-huayuan.txt；全量环境常已间接具备

# ---------- [华院保留] HTTP / YAML / 条件边 ----------
httpx>=0.24.0,<1.0.0
pyyaml>=6.0.0,<7.0.0
simpleeval>=0.9.13,<1.0.0

# ---------- [开发] ----------
pytest>=7.4.0,<8.0.0
pytest-asyncio>=0.21.0,<1.0.0
pytest-mock>=3.11.0,<4.0.0
pytest-cov>=4.1.0,<5.0.0
black>=23.0.0,<24.0.0
ruff>=0.1.0,<1.0.0
mypy>=1.5.0,<2.0.0

# ---------- [华院可删] 测试用 SQLite ----------
aiosqlite>=0.19.0,<1.0.0

# ---------- [可选] 可观测（华院镜像默认不装；代码 lazy import） ----------
langfuse>=3.0.0,<4.0.0

# ---------- [华院可删] 主库 ID ----------
python-ulid>=1.1.0,<2.0.0

# ---------- [华院可删] RAG 导入 / 本地 embedding / pgvector ----------
pandas>=2.0.0,<3.0.0
openpyxl>=3.1.0,<4.0.0
sentence-transformers>=2.2.0,<3.0.0
pgvector>=0.2.0,<1.0.0
volcengine-python-sdk[ark]>=5.0.0,<6.0.0

# ---------- [华院保留] 新闻 URL 抓取 ----------
# SDK 是 CLI 子进程模型：还需主机/镜像内有 claude 可执行文件（见 Dockerfile）
claude-agent-sdk>=0.1.0,<1.0.0

# ---------- [worker] 雷达新闻知识库写入（scripts/news_content_worker.py） ----------
# exhibition MySQL 与主库 PG 是不同实例；华院 API 镜像可不装本组。
pymysql>=1.1.0,<2.0.0
# MySQL 8 caching_sha2_password 需要 cryptography
cryptography>=41.0.0
# ---------- [华院保留 + worker] Milvus（勿降到 2.x） ----------
pymilvus>=3.0.1,<4.0.0
