# 目的
将各类来源的数据清洗到我的向量库中。
将各类来源的基础数据清洗到向量库中，同时支持数据的运行。

# 核心数据体
## 数据验证集
数据验证集的格式应该是可以执行的一些元数据

- 如何使用？
    作为测试case，测试模型的回复是否能够达到要求

## 知识库
- 作用
    作为流程回答问题的知识库，提高模型回复的质量
- 知识库的核心字段
    - embedding字段。将场景、问题、回复组装为一个自然文本，然后交友embedding模型进行向量化
    - 场景、问题、回复的提取字段。这些字段都是预处理后文本
    - 原始素材。通常这里记录的是关联关系


# 核心流程

## 知识库的数据转换流程
- 流程要点：
    - 原始数据集的抽取
    - 抽取后的数据的清洗。聊天类的数据清洗的方式为：将问题场景、问题改写、回复给清洗出来
    - 数据入库。这里的数据结构有讲究，非常重要的一点是如何进行数据召回

-------

- 20260130 前的设计

# 当前rag的构建思路
1. 首先将static下的rag_source/uat_data的excel，进行向量化后存入vector库中。核心提取内容为场景摘要和优化问题
2. 然后将运行是的流程的前置节点也增加场景摘要和优化问题的提取能力，然后调用相同版本的embedding模型，召回相似的回答案例，喂给模型，让其能够回答好问题。

# 任务拆分
1. 数据向量化
    - 数据向量化流程代码的编写（ok）
    - 运行流程代码的脚本（ok）
    - 发现的bug
        - 向量表的原始embedding的文字为存储，需要重新构建，以保障后续可维护
        - 脚本的版本控制字段未实际使用，需要继续完善
    - 总结全量的场景，以及我当前的向量库里面的场景情况。**这个需要调研一下，再决定怎么做**
2. 流程的前置节点提示词升级（26日开始）
    - 将rag的数据接入到提示词中，使用召回的方式测试
        - 测试的方法：选择你需要的场景，mock你的数据，然后进行测试看看是否可行
        - 

3. 流程的向量召回相关节点开发
4. 以及后续的测试
