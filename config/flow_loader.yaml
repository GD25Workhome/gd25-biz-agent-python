# 流程加载配置（华院最小部署）
# 仅预加载华院三条业务流，避免启动时编译医疗/RAG 流程

flows:
  preload:
    - huayuan_simple_agent
    - huayuan_react_agent
    - huayuan_radar_event_agent

  lazy_load:
    - huayuan_radar_event_agent_v1_single_react
